# Retrieval-Augmented Generation

RAG combines information retrieval with LLM generation.

Instead of relying only on model memory, the system retrieves relevant documents before generating a response.

This improves accuracy, reduces hallucinations, and enables domain-specific AI products.